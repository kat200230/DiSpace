# Универсальный скрипт

import { useState } from 'react';
import useBaseUrl from '@docusaurus/useBaseUrl';
import { DiscordMention, DiscordMessage, DiscordMessages } from '@danktuary/react-discord-message';
import DiPeekResponse from '@site/src/components/DiPeekResponse';

:::info Пререквизиты
Минимальнейшие навыки веб-разработки, а конкретнее, -отладки.
:::

:::danger Что-то странное происходит с DiSpace
Иногда тесты не находятся, их будто бы и не было. Иногда некоторые переменные не инициализируются, и вообще всякий бред происходит. Так что знайте, что этот способ теперь __не гарантирует получение результатов__. Подготовьтесь хотя бы немного к тесту, на случай если скрипт не сработает.
:::

### Этим скриптом можно получить **все\*** ответы на **любой** тест.

**\*** ... кроме ответов на открытые вопросы, которые преподаватели лично проверяют и выставляют оценку.  
Их [ищите по айди вопроса](./searching-questions-by-id) в базе данных DiPeek'а.

<br/><br/>

Как это работает? Всё просто. Когда вы начинаете проходить тест, DiSpace торопится и сразу же заносит вашу незавершённую попытку прохождения в свою базу данных. Оттуда её, вместе со всеми ответами на попавшиеся вам вопросы, можно легко выловить методом тыка, если знать где конкретно смотреть.

:::danger Внимание
Внимательно прочитайте и изучите этот метод перед его применением!  
Я ещё никого не встречал, кто мог разобраться с этим методом на лету, за пару минут.
:::

### Процедура поиска {#search-procedure}

1. [Узнаём значение ИДЫ](#finding-identifier-approximus), и после этого сразу начинаем тест.
2. Открываем инструменты разработчика, вкладку "Консоль" (Ctrl+Shift+J).
3. Вставляем [скрипт](#script-code) и пишем `dipeek_search(IDA, 200)` с полученным значением ИДЫ.
4. Как только высветится результат, переходим на страницу вопроса.
5. Вставляем скрипт снова и пишем `dipeek_show_question()`.
6. Будет выведен объект вопроса в JSON-образном формате**\***.
7. Повторяйте шаги 4-6, пока не пройдёте весь тест.

**\*** Рекомендую потренироваться в тренировочном режиме скрипта.

### *Тренировочный режим* {#training-mode}

1. Откройте страницу DiSpace, любую.
2. Открываем инструменты разработчика, вкладку "Консоль" (Ctrl+Shift+J).
3. Вставляет [скрипт](#script-code) и пишем `dipeek_train()`.
4. Как только высветится результат, пишем `dipeek_show_question()`.
5. Будет выведен объект вопроса в JSON-образном формате.  
**Поизучайте его и посмотрите как он работает.**
6. Повторяйте шаги 4-5, пока не пройдёте весь тест.

### Узнаём аппроксимус идентификатора (ИДУ) {#finding-identifier-approximus}

Аппроксимус идентификатора (*identifier approximus*, сокращение **ИДА**) - приблизительное значение идентификатора вашей попытки прохождения теста. Так как чуть ли не постоянно кто-то да проходит какой-то тест, это значение медленно растёт. Например, на момент написания значение равно 1041700.

:::caution Предупреждение
Узнавать его надо только прямо перед началом прохождения теста!  
Если задержитесь на час, ИДА может сместиться, и придётся узнавать заново.
:::

Узнать его значение можно у веб-скрейперов DiSpace. На [Discord сервере DiPeek'а](https://discord.gg/tphsh9vsty) у них стоит особенная роль "Веб-скрейпер", обращайтесь к ним. Также, в канале #📝ida вы можете увидеть недавно определённые ИДЫ. Если они были определены относительно недавно, можете использовать их.

:::info Самостоятельный веб-скрейпинг
Вы можете научиться проводить веб-скрейпинг DiSpace самостоятельно! [Читайте дальше](#standalone-webscraping).
:::

### Код скрипта {#script-code}

import CodeSnippet from '@site/src/components/CodeSnippet';
import Script from '!!raw-loader!@site/../DiSpaceExtractor/WebEx.js';

<details>
  <summary>Показать/скрыть</summary>
<CodeSnippet title="нет, это не обфускация; это просто сжатие.">{Script}</CodeSnippet>
</details>

### Самостоятельный веб-скрейпинг DiSpace {#standalone-webscraping}

Если веб-скрейперы заняты, вы можете получить значение ИДЫ самостоятельно.  
Скопируйте следующий код и вставьте его в консоль:

```js title="нет, это не обфускация; это просто сжатие."
function _g(i){
  return new Promise(r=>$.ajax({
    type:'POST',url:'/ditest/index/result',
    data:{action:'get_results_by_test',score_id:i,test_id:39998},
    dataType:'json',success:r,
    error:(x,s,e)=>r({error:`ExtractionError: ${e} (${s})`})
  }))}
async function getDate(i){
  let j=0,m=20;while(j<m){let res=await _g(i+j++);
    if(res&&!res.error)return res.simple.test_start_time;}}
```

Затем вводите `await getDate(IDA)` с разными значениями `IDA`, пока не найдёте крайнее значение, начиная с которого дату не удаётся получить (а то есть попытки с таким айди не существует ещё).

Например, можно начать с `1000000` (одного миллиона) или последнего известного значения ИДЫ. И потом прибавлять по `1000` за каждый день разницы от настоящей даты. Это алгоритм двоичного поиска, по сути, но использующий интуицию пользователя вместо деления на 2. Выбирайте такое значение, которое, как вам кажется, будет ближе к истинному.

Критерии истинного значения ИДЫ на момент времени:  
— `await getDate(IDA - 100)` возвращает какую-то дату;  
— `await getDate(IDA)` - возвращает `undefined`;